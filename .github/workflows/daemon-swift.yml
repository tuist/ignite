name: Daemon Swift

on:
  push:
    branches: [main]
    paths:
      - "daemon-swift/**"
      - ".github/workflows/daemon-swift.yml"
  pull_request:
    branches: [main]
    paths:
      - "daemon-swift/**"
      - ".github/workflows/daemon-swift.yml"

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - uses: jdx/mise-action@f20a77c9fddb7001d2af929298f7807cb2014520

      - name: Restore SPM cache
        uses: actions/cache@v4
        with:
          path: daemon-swift/.build
          key: ${{ runner.os }}-spm-${{ hashFiles('daemon-swift/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Build
        run: mise run build:daemon-swift

  test:
    name: Test
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - uses: jdx/mise-action@f20a77c9fddb7001d2af929298f7807cb2014520

      - name: Restore SPM cache
        uses: actions/cache@v4
        with:
          path: daemon-swift/.build
          key: ${{ runner.os }}-spm-${{ hashFiles('daemon-swift/Package.resolved') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Run tests
        run: mise run test:daemon-swift

  lint:
    name: Lint
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - uses: jdx/mise-action@f20a77c9fddb7001d2af929298f7807cb2014520

      - name: Check formatting with SwiftLint
        run: mise run lint:daemon-swift
